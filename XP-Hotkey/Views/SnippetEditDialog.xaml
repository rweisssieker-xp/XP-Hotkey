<Window x:Class="XP_Hotkey.Views.SnippetEditDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Snippet bearbeiten" Height="650" Width="700"
        WindowStartupLocation="CenterOwner">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="200"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Content="Kürzel:"/>
        <TextBox Grid.Row="1" Name="ShortcutTextBox" Margin="0,5,0,10"/>

        <Label Grid.Row="2" Content="Text:"/>
        <TextBox Grid.Row="3" Name="TextTextBox" 
                  TextWrapping="Wrap" AcceptsReturn="True"
                  VerticalScrollBarVisibility="Auto" Margin="0,5,0,10"/>

        <Label Grid.Row="4" Content="Beschreibung:"/>
        <TextBox Grid.Row="5" Name="DescriptionTextBox" Margin="0,5,0,10"/>

        <StackPanel Grid.Row="6" Orientation="Vertical" Margin="0,10">
            <CheckBox Name="UseRegexCheckBox" Content="Regex verwenden (Kürzel ist regulärer Ausdruck)" Margin="0,5"/>
            <CheckBox Name="CaseSensitiveCheckBox" Content="Groß-/Kleinschreibung beachten" Margin="0,5"/>
            <CheckBox Name="EnabledCheckBox" Content="Snippet aktiviert" IsChecked="True" Margin="0,5"/>

            <StackPanel Orientation="Horizontal" Margin="0,10,0,0">
                <Label Content="Hotkey (optional):" Width="120"/>
                <TextBox Name="HotkeyTextBox" Width="200" ToolTip="z.B. Ctrl+Shift+K"/>
            </StackPanel>
        </StackPanel>

        <!-- App-spezifische Filter -->
        <Expander Grid.Row="7" Header="App-spezifische Filter (optional)" IsExpanded="False" Margin="0,10">
            <Grid Margin="10">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="20"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <!-- Allowed Apps -->
                <StackPanel Grid.Column="0">
                    <Label Content="Nur in diesen Apps erlaubt:" FontWeight="Bold"/>
                    <TextBlock Text="(Leer = in allen Apps erlaubt)" FontStyle="Italic" Margin="0,0,0,5"/>
                    <ListBox Name="AllowedAppsListBox" Height="120" Margin="0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Button Content="Hinzufügen" Click="AddAllowedApp_Click" Width="90" Margin="0,0,5,0"/>
                        <Button Content="Entfernen" Click="RemoveAllowedApp_Click" Width="90"/>
                    </StackPanel>
                </StackPanel>

                <!-- Blocked Apps -->
                <StackPanel Grid.Column="2">
                    <Label Content="In diesen Apps blockiert:" FontWeight="Bold"/>
                    <TextBlock Text="(Snippet wird nicht expandiert)" FontStyle="Italic" Margin="0,0,0,5"/>
                    <ListBox Name="BlockedAppsListBox" Height="120" Margin="0,5"/>
                    <StackPanel Orientation="Horizontal" Margin="0,5">
                        <Button Content="Hinzufügen" Click="AddBlockedApp_Click" Width="90" Margin="0,0,5,0"/>
                        <Button Content="Entfernen" Click="RemoveBlockedApp_Click" Width="90"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Expander>

        <StackPanel Grid.Row="8" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10">
            <Button Content="OK" Click="OkButton_Click" Width="75" Margin="5,0"/>
            <Button Content="Abbrechen" Click="CancelButton_Click" Width="75" Margin="5,0"/>
        </StackPanel>
    </Grid>
    </ScrollViewer>
</Window>

